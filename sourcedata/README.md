# sourcedata — original distribution

Unmodified original files for this dataset as distributed upstream, retrieved via MOABB.
Included so the deposit carries its own provenance instead of depending on an external
URL remaining reachable.

| | |
|---|---|
| MOABB class | `moabb.datasets.Rodrigues2017` (moabb 1.5.0) |
| Retrieved | 2026-08-11 |
| Files | 19 |
| Total size | 101.3 MB |
| License | CC-BY-4.0 |
| Upstream DOI | https://doi.org/10.5281/zenodo.2348892 |

File contents are byte-for-byte as retrieved; no conversion was applied. Filenames are
the upstream distribution names (e.g. `subject_01.mat` from
`zenodo.org/record/2348892/files/`); the local download cache wraps each file in a
`<name>/<md5>-<name>` directory, and that cache-only prefix/wrapper was removed so the
tree matches what upstream actually publishes.
Per-file SHA-256 checksums are in `sourcedata_provenance.json`.
